# Migration `20200305152742-init`

This migration has been generated by ILshat Khamitov at 3/5/2020, 3:27:42 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."auth_users" (
    "date_joined" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "date_of_birth" DATE   ,
    "email" TEXT NOT NULL DEFAULT '' ,
    "first_name" TEXT   ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "is_active" BOOLEAN NOT NULL DEFAULT false ,
    "is_staff" BOOLEAN NOT NULL DEFAULT false ,
    "is_superuser" BOOLEAN NOT NULL DEFAULT false ,
    "last_login" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "last_name" TEXT   ,
    "password" TEXT NOT NULL DEFAULT '' ,
    "username" TEXT   
) 

CREATE UNIQUE INDEX "quaint"."auth_users.email" ON "auth_users"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200305152742-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,27 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "sqlite"
+  url      = "file:./data.db"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id          Int       @default(autoincrement()) @id
+  email       String    @unique
+  username    String?
+  password    String
+  firstName   String?   @map("first_name")
+  lastName    String?   @map("last_name")
+  lastLogin   DateTime  @updatedAt @map("last_login")
+  isSuperuser Boolean   @default(false) @map("is_superuser")
+  isStaff     Boolean   @default(false) @map("is_staff")
+  isActive    Boolean   @default(false) @map("is_active")
+  dateJoined  DateTime  @default(now()) @map("date_joined")
+  dateOfBirth DateTime? @map("date_of_birth")
+  @@map("auth_users")
+}
```


